fastapi==0.141.1
# Pinned explicitly, not left to fastapi's resolver — fastapi>=0.133.0 no
# longer caps the starlette version at all, so without this pin here the
# actual installed starlette version would silently drift on every fresh
# install. 1.6.0 clears every CVE pip-audit flagged against 0.41.3
# (PYSEC-2026-161/248/249/1941/1942/2280/2281), the highest fix version
# among them being 1.3.1.
starlette==1.6.0
uvicorn[standard]==0.34.0
pydantic==2.10.4
pyyaml==6.0.2
psycopg[binary]==3.2.3

# §15.1/D51 — Tier 2 bootstrap fitting (training.py) and serving (model.py).
# xgboost is deliberately NOT pinned here: PDM_MODEL_FAMILY=xgboost is
# config-gated (§14.2.2/D20) and only imported lazily inside
# training.py::_build_estimator when actually selected — installing it
# unconditionally would add a large, unused dependency to every image that
# stays on the random_forest default.
scikit-learn==1.8.0
pandas==2.3.3
joblib==1.5.3
