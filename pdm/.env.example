# ── PdM Python service configuration ────────────────────────────────────
# Currently the service takes no runtime config of its own — thresholds
# live in app/thresholds.yaml, and the port is fixed at 8000 (see
# Dockerfile / docker-compose.pdm.yml). Reserved for future use (e.g. a
# TIER2_MODEL_PATH once model.py has something to load).
